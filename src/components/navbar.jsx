import React from "react";
import Logo from '../assets/logo.png';

const Navbar = () => {
    return (
        <nav>
            <img src={Logo} alt="Logo" />
            {}
        </nav>
    );
}

export default Navbar;
